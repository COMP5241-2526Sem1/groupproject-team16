# 环境变量示例文件
# 复制此文件为 .env.development 和 .env.production 并根据需要修改

# 前端 API 基础 URL
# 本地开发: http://localhost:3001/api (直接连接后端服务器)
# Vercel 部署: /api (使用相对路径，由 vercel.json rewrites 处理)
# 如果不设置，会自动根据环境选择
# VITE_API_BASE_URL=/api
