# Agent教学管理后台 - 权限系统说明

## 📋 角色定义

系统支持三种用户角色,每种角色拥有不同的权限:

### 1. 教师 (TEACHER)
- **课程查看**: 只能看到自己创建的课程
- **课程管理**: 可以创建、编辑、删除自己的课程
- **课程内容**: 对自己的课程拥有完全管理权限
- **学生管理**: 可以查看和管理自己课程的学生

### 2. 学生 (STUDENT)
- **课程查看**: 只能看到已加入的课程
- **课程加入**: 可以浏览并加入可用课程
- **课程参与**: 可以参与课程活动(作业、讨论、测验等)
- **权限限制**: 不能创建、编辑或删除课程

### 3. 管理员 (ADMIN)
- **全局查看**: 可以看到所有课程
- **全局管理**: 可以创建、编辑、删除任何课程
- **用户管理**: 可以管理所有用户和权限
- **系统配置**: 拥有最高权限

---

## 🔐 权限矩阵

| 功能 | 教师 | 学生 | 管理员 |
|------|------|------|--------|
| 查看自己的课程 | ✅ | ✅ | ✅ |
| 查看所有课程 | ❌ | ❌ | ✅ |
| 创建课程 | ✅ | ❌ | ✅ |
| 编辑自己的课程 | ✅ | ❌ | ✅ |
| 编辑他人的课程 | ❌ | ❌ | ✅ |
| 删除自己的课程 | ✅ | ❌ | ✅ |
| 删除他人的课程 | ❌ | ❌ | ✅ |
| 加入课程 | ❌ | ✅ | ✅ |
| 发布作业 | ✅ | ❌ | ✅ |
| 提交作业 | ❌ | ✅ | ✅ |
| 创建测验 | ✅ | ❌ | ✅ |
| 参加测验 | ❌ | ✅ | ✅ |
| 发起讨论 | ✅ | ✅ | ✅ |
| 回复讨论 | ✅ | ✅ | ✅ |
| 查看数据分析 | ✅ | ❌ | ✅ |

---

## 🎯 使用场景

### 教师场景

**张教授(教师)登录后:**

1. **课程列表页**
   - 只显示"机器学习基础"课程(自己创建的)
   - 显示"创建新课程"按钮
   - 每个课程卡片显示"编辑"和"删除"按钮

2. **创建课程**
   - 点击"创建新课程"
   - 填写课程信息(教师字段自动填充为自己的名字)
   - 创建后立即出现在课程列表中

3. **管理课程**
   - 可以编辑课程信息
   - 可以删除课程
   - 可以发布作业和测验
   - 可以查看学生提交和成绩

---

### 学生场景

**李明(学生)登录后:**

1. **课程列表页**
   - 初始为空(还未加入任何课程)
   - 显示"加入课程"按钮
   - 不显示"创建新课程"按钮

2. **加入课程**
   - 点击"加入课程"按钮
   - 浏览所有可用课程列表
   - 点击"加入"按钮加入感兴趣的课程

3. **参与课程**
   - 加入后课程出现在列表中
   - 可以进入课程查看内容
   - 可以提交作业、参加测验、发起讨论
   - 不能编辑或删除课程

---

### 管理员场景

**系统管理员登录后:**

1. **课程列表页**
   - 显示所有课程(包括所有教师创建的)
   - 显示"创建新课程"按钮
   - 所有课程都显示"编辑"和"删除"按钮

2. **全局管理**
   - 可以创建任何课程
   - 可以编辑任何课程
   - 可以删除任何课程
   - 可以管理所有用户和权限

---

## 🔧 技术实现

### 前端权限控制

```javascript
// 获取当前用户
const currentUser = JSON.parse(localStorage.getItem('user'))

// 检查管理权限
const hasManagePermission = (course) => {
  if (currentUser.role === 'ADMIN') return true
  if (currentUser.role === 'TEACHER' && course.teacherId === currentUser.id) return true
  return false
}

// 过滤课程列表
const getFilteredCourses = () => {
  if (currentUser.role === 'TEACHER') {
    return courses.filter(course => course.teacherId === currentUser.id)
  } else if (currentUser.role === 'STUDENT') {
    return courses.filter(course => course.enrolledStudents.includes(currentUser.id))
  }
  return courses // 管理员看到所有课程
}
```

### 条件渲染

```jsx
{/* 只有教师和管理员显示创建按钮 */}
{(currentUser.role === 'TEACHER' || currentUser.role === 'ADMIN') && (
  <Button onClick={handleOpenCreate}>创建新课程</Button>
)}

{/* 只有学生显示加入按钮 */}
{currentUser.role === 'STUDENT' && (
  <Button onClick={handleJoinCourse}>加入课程</Button>
)}

{/* 只有有管理权限的用户显示编辑删除按钮 */}
{hasManagePermission(course) && (
  <>
    <Button onClick={() => handleEdit(course)}>编辑</Button>
    <Button onClick={() => handleDelete(course)}>删除</Button>
  </>
)}
```

---

## 🧪 测试说明

### 测试不同角色

1. **测试教师角色**
   - 登录页选择"教师"快速登录
   - 验证只能看到自己的课程
   - 验证可以创建、编辑、删除课程

2. **测试学生角色**
   - 登录页选择"学生"快速登录
   - 验证初始课程列表为空
   - 验证可以加入课程
   - 验证不能编辑删除课程

3. **测试管理员角色**
   - 登录页选择"管理员"快速登录
   - 验证可以看到所有课程
   - 验证可以管理任何课程

---

## 📝 数据结构

### 用户对象
```javascript
{
  id: 'user123',
  name: '张教授',
  email: 'zhang@example.com',
  role: 'TEACHER', // 'TEACHER' | 'STUDENT' | 'ADMIN'
  avatar: 'https://...'
}
```

### 课程对象
```javascript
{
  id: 1,
  name: '机器学习基础',
  description: '...',
  teacher: '张教授',
  teacherId: 'user123', // 创建者ID
  students: 45,
  enrolledStudents: ['student1', 'student2'], // 已加入的学生ID列表
  status: 'active',
  progress: 65
}
```

---

## 🚀 未来扩展

### 后端集成
- 连接后端API进行权限验证
- JWT token验证
- 角色基于数据库存储

### 更细粒度权限
- 助教角色(TA)
- 课程协作者
- 只读访客

### 权限组
- 自定义权限组
- 权限继承
- 动态权限分配

---

## ⚠️ 注意事项

1. **前端权限仅用于UI控制**
   - 当前权限控制仅在前端实现
   - 真实环境需要后端API验证
   - 避免在前端存储敏感数据

2. **数据持久化**
   - 当前使用localStorage存储
   - 生产环境应使用数据库
   - 需要实现数据同步机制

3. **安全性**
   - 前端权限可被绕过
   - 必须在后端进行权限验证
   - 使用HTTPS传输敏感数据

