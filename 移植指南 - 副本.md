# 文件上传功能移植指南

本文档说明如何将这个文件上传功能移植到其他项目中。

## 📦 核心文件清单

### 后端核心文件（必须）

1. **`oss-client.js`** - OSS 客户端封装类（核心）
2. **`oss-config.js`** - OSS 配置文件
3. **路由代码** - 从 `server.js` 中提取的 4 个 API 路由

### 前端核心文件（可选）

1. **`App.js`** - React 组件（如果使用 React）
2. **`App.css`** - 样式文件

---

## 🔧 后端移植步骤

### 步骤 1: 安装依赖

在你的项目 `package.json` 中添加：

```json
{
  "dependencies": {
    "ali-oss": "^6.20.0",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
```

然后运行：
```bash
npm install
```

### 步骤 2: 复制核心文件

#### 2.1 复制 `oss-client.js`
将整个文件复制到你的项目中，这个文件是独立的，不依赖其他代码。

#### 2.2 复制 `oss-config.js`
复制配置文件，并根据你的 OSS 信息修改配置。

### 步骤 3: 集成路由代码

从 `server.js` 中提取以下 4 个路由，集成到你的项目中：

#### 3.1 初始化代码（放在路由之前）

```javascript
const express = require('express');
const multer = require('multer');
const fs = require('fs');
const path = require('path');
const OSSClient = require('./oss-client');
const ossConfig = require('./oss-config');

// 配置临时上传目录
const UPLOADS_DIR = path.join(__dirname, 'uploads');
if (!fs.existsSync(UPLOADS_DIR)) {
  fs.mkdirSync(UPLOADS_DIR, { recursive: true });
}

// 初始化 OSS 客户端
const ossClient = new OSSClient(
  ossConfig.accessKeyId,
  ossConfig.accessKeySecret,
  ossConfig.bucket,
  ossConfig.region,
  ossConfig.prefix
);

// 配置 multer
const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, UPLOADS_DIR);
  },
  filename: (req, file, cb) => {
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
    const ext = path.extname(file.originalname);
    const name = path.basename(file.originalname, ext);
    cb(null, `${name}-${uniqueSuffix}${ext}`);
  }
});

const upload = multer({ 
  storage: storage,
  limits: { fileSize: 100 * 1024 * 1024 } // 100MB
});

// 文件数据存储（如果使用 JSON，否则使用数据库）
const FILES_DATA_FILE = path.join(__dirname, 'files-data.json');

function loadFilesData() {
  try {
    if (fs.existsSync(FILES_DATA_FILE)) {
      const data = fs.readFileSync(FILES_DATA_FILE, 'utf8');
      return JSON.parse(data);
    }
  } catch (error) {
    console.error('Error loading files data:', error);
  }
  return [];
}

function saveFilesData(files) {
  try {
    fs.writeFileSync(FILES_DATA_FILE, JSON.stringify(files, null, 2), 'utf8');
  } catch (error) {
    console.error('Error saving files data:', error);
  }
}

let files = loadFilesData();
```

#### 3.2 路由 1: 上传文件

```javascript
app.post('/api/upload', upload.single('file'), async (req, res) => {
  const file = req.file;
  if (!file) {
    return res.status(400).json({ error: 'No file uploaded' });
  }

  try {
    // 生成唯一文件名
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
    const ext = path.extname(file.originalname);
    const name = path.basename(file.originalname, ext);
    const ossFileName = `${name}-${uniqueSuffix}${ext}`;

    // 上传到 OSS
    const uploadResult = await ossClient.uploadFile(file.path, ossFileName);

    // 删除本地临时文件
    if (fs.existsSync(file.path)) {
      fs.unlinkSync(file.path);
    }

    // 生成文件ID
    const fileId = files.length > 0 ? Math.max(...files.map(f => f.id)) + 1 : 1;

    // 获取 OSS 文件访问 URL
    const fileUrl = await ossClient.getFileUrl(ossFileName, 3600);

    // 保存文件信息
    const fileInfo = {
      id: fileId,
      fileName: file.originalname,
      filePath: ossFileName,
      fileUrl: fileUrl,
      ossUrl: uploadResult.url,
      fileSize: file.size,
      uploadTime: new Date().toISOString(),
    };

    files.push(fileInfo);
    saveFilesData(files); // 如果使用数据库，改为数据库保存

    res.json({ 
      success: true,
      message: 'File uploaded successfully to OSS',
      file: fileInfo
    });
  } catch (error) {
    console.error('Upload error:', error);
    if (file && fs.existsSync(file.path)) {
      fs.unlinkSync(file.path);
    }
    res.status(500).json({ error: 'Upload failed', details: error.message });
  }
});
```

#### 3.3 路由 2: 获取文件列表

```javascript
app.get('/api/files', (req, res) => {
  files = loadFilesData(); // 如果使用数据库，改为数据库查询
  res.json({ 
    success: true,
    count: files.length,
    files: files.map(f => ({
      id: f.id,
      fileName: f.fileName,
      fileUrl: f.fileUrl,
      fileSize: f.fileSize,
      uploadTime: f.uploadTime
    }))
  });
});
```

#### 3.4 路由 3: 下载文件（通过ID）

```javascript
app.get('/api/download/:id', async (req, res) => {
  const fileId = parseInt(req.params.id);
  
  try {
    const currentFiles = loadFilesData(); // 如果使用数据库，改为数据库查询
    const file = currentFiles.find(f => f.id === fileId);
    
    if (!file) {
      return res.status(404).json({ error: 'File not found' });
    }

    // 检查 OSS 中文件是否存在
    const exists = await ossClient.fileExists(file.filePath);
    if (!exists) {
      return res.status(404).json({ error: 'File not found in OSS' });
    }

    // 从 OSS 获取文件流
    const fileStream = await ossClient.getFileStream(file.filePath);

    // 设置下载响应头
    res.setHeader('Content-Disposition', `attachment; filename="${encodeURIComponent(file.fileName)}"`);
    res.setHeader('Content-Type', 'application/octet-stream');

    // 将流管道到响应
    fileStream.stream.pipe(res);
  } catch (error) {
    console.error('Download error:', error);
    res.status(500).json({ error: 'Download failed', details: error.message });
  }
});
```

#### 3.5 路由 4: 删除文件

```javascript
app.delete('/api/files/:id', async (req, res) => {
  const fileId = parseInt(req.params.id);
  
  try {
    files = loadFilesData(); // 如果使用数据库，改为数据库查询
    const fileIndex = files.findIndex(f => f.id === fileId);
    
    if (fileIndex === -1) {
      return res.status(404).json({ error: 'File not found' });
    }

    const file = files[fileIndex];

    // 从 OSS 删除文件
    await ossClient.deleteFile(file.filePath);

    // 从数组中删除（如果使用数据库，改为数据库删除）
    files.splice(fileIndex, 1);
    saveFilesData(files);

    res.json({ 
      success: true,
      message: 'File deleted successfully from OSS'
    });
  } catch (error) {
    console.error('Delete error:', error);
    res.status(500).json({ error: 'Delete failed', details: error.message });
  }
});
```

---

## 🎨 前端移植步骤（React 示例）

### 步骤 1: 复制核心函数

从 `App.js` 中提取以下核心函数：

```javascript
// API 基础 URL（根据你的项目修改）
const API_URL = 'http://localhost:5000';

// 获取文件列表
const fetchFiles = async () => {
  const response = await axios.get(`${API_URL}/api/files`);
  return response.data.files || [];
};

// 上传文件
const handleUpload = async (file) => {
  const formData = new FormData();
  formData.append('file', file);
  
  const response = await axios.post(`${API_URL}/api/upload`, formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  });
  
  return response.data;
};

// 下载文件
const handleDownload = (fileId) => {
  window.open(`${API_URL}/api/download/${fileId}`, '_blank');
};

// 删除文件
const handleDelete = async (fileId) => {
  const response = await axios.delete(`${API_URL}/api/files/${fileId}`);
  return response.data;
};
```

### 步骤 2: 集成到你的组件

将这些函数集成到你的 React 组件中，或者封装成自定义 Hook。

---

## 🔄 使用数据库替代 JSON 存储

如果你使用数据库（MongoDB、PostgreSQL 等），需要修改以下部分：

### 替换文件信息存储

**原代码（JSON）：**
```javascript
files.push(fileInfo);
saveFilesData(files);
```

**改为数据库（示例 - MongoDB）：**
```javascript
// 保存到数据库
await FileModel.create(fileInfo);
```

**原代码（JSON）：**
```javascript
files = loadFilesData();
const file = files.find(f => f.id === fileId);
```

**改为数据库：**
```javascript
const file = await FileModel.findById(fileId);
```

---

## 📋 关键依赖关系

```
server.js (路由)
  ├── 依赖 oss-client.js (OSS 操作)
  ├── 依赖 oss-config.js (配置)
  ├── 依赖 multer (文件上传处理)
  └── 依赖 express (Web 框架)

oss-client.js
  └── 依赖 ali-oss (阿里云 OSS SDK)
```

---

## ⚠️ 注意事项

1. **OSS 配置**: 确保 `oss-config.js` 中的配置正确
2. **临时目录**: 确保 `uploads/` 目录有写入权限
3. **文件大小**: 默认限制 100MB，可在 multer 配置中修改
4. **错误处理**: 根据你的项目需求调整错误处理逻辑
5. **数据库**: 如果使用数据库，需要创建对应的数据模型
6. **权限控制**: 生产环境需要添加用户认证和权限控制

---

## 🎯 最小化移植（只保留核心功能）

如果只需要核心功能，最小化移植需要：

1. **`oss-client.js`** - 必须
2. **`oss-config.js`** - 必须
3. **上传路由** - 必须
4. **下载路由** - 必须

可选：
- 文件列表路由
- 删除路由
- 前端代码

---

## 📝 快速检查清单

移植完成后，检查以下项目：

- [ ] 已安装 `ali-oss` 依赖
- [ ] 已配置 OSS 信息（`oss-config.js`）
- [ ] 已创建临时上传目录
- [ ] 路由已正确集成
- [ ] 错误处理已实现
- [ ] 文件信息存储已配置（JSON 或数据库）
- [ ] CORS 已配置（如果前端在不同域名）

---

## 🔗 API 接口总结

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/upload` | 上传文件到 OSS |
| GET | `/api/files` | 获取文件列表 |
| GET | `/api/download/:id` | 通过ID下载文件 |
| DELETE | `/api/files/:id` | 删除文件 |

---

## 💡 提示

- **模块化**: 可以将路由代码提取到单独的路由文件中
- **中间件**: 可以添加认证中间件来保护路由
- **验证**: 可以添加文件类型和大小验证
- **日志**: 可以添加更详细的日志记录

