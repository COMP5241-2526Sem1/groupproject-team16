# 开发运行指南

## 🚀 快速启动

### 1. 安装依赖
```bash
# 安装前端和后端所有依赖
npm run install:all
```

### 2. 启动开发服务器

#### 方法一：同时启动前后端（推荐）
```bash
npm run dev:full
```

#### 方法二：分别启动
```bash
# 终端1：启动后端服务器
npm run server:dev

# 终端2：启动前端开发服务器
npm run dev
```

## 📱 访问地址

- **前端应用**: http://localhost:5173 (如端口被占用会自动切换到5174等)
- **后端API**: http://localhost:3001
- **API文档**: http://localhost:3001/ (查看所有可用端点)

## 🎯 测试AI功能

### 1. 访问AI代理生成页面
- 打开浏览器访问前端地址
- 导航到 "课程管理" → "AI代理生成"
- 或直接访问: http://localhost:5173/ 或 http://localhost:5174/ (取决于实际端口)

### 2. 使用示例数据快速测试
页面上有三个示例按钮，点击即可快速填充测试数据：
- 机器学习入门
- Web前端开发  
- 数据结构与算法

### 3. 自定义生成
填写表单字段：
- **课程主题**: 如"高等数学基础"
- **知识点大纲**: 输入详细的课程大纲
- **课程时长**: 如"14周"
- **目标学员**: 选择级别
- **其他要求**: 可选的额外需求

## 🎨 新功能：美化课程大纲显示

现在课程大纲会以更美观的格式显示：
- ✅ 自动识别章节编号（1. 2. 3. ...）
- ✅ 章节标题突出显示
- ✅ 内容描述格式化
- ✅ 渐变背景和边框效果
- ✅ 可滚动的长内容区域
- ✅ 响应式设计

## 🛠️ 开发工具

### 数据库管理
```bash
# 查看数据库结构
npm run db:studio

# 生成Prisma客户端
npm run db:generate

# 推送数据库更改
npm run db:push
```

### API测试
```bash
# 测试AI API端点
curl -X POST http://localhost:3001/api/ai/chat \
  -H "Content-Type: application/json" \
  -d '{"system":"你是一个课程设计专家","user":"生成Python课程大纲"}'
```

## 📋 可用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动前端开发服务器 |
| `npm run server:dev` | 启动后端开发服务器 |
| `npm run dev:full` | 同时启动前后端 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产构建 |
| `npm run lint` | 代码检查 |

## 🐛 常见问题

### 端口冲突
- 前端默认5173端口，如被占用会自动切换到5174等
- 后端固定3001端口，如冲突请检查其他进程

### API调用失败
- 确保后端服务器已启动
- 检查控制台是否有CORS错误
- 验证API端点地址是否正确

### AI生成失败
- 检查Doubao API密钥配置
- 查看后端日志中的错误信息
- 确认网络连接正常

## 📁 项目结构

```
├── src/                    # 前端源码
│   ├── components/         # React组件
│   │   ├── AgentGenerator.jsx  # AI课程生成器（已美化）
│   │   └── ...
│   └── ...
├── server/                 # 后端源码
│   ├── routes/            # API路由
│   ├── utils/             # 工具函数
│   │   └── doubao_api.js  # AI API封装
│   └── index.js           # 服务器入口
└── ...
```